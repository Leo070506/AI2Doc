# AI2Doc Templates

该目录将保存可分发、可版本化的 Pandoc/DOCX 模板资产及其元数据。

Phase 1 只实现一个默认模板。模板系统必须遵循：

- 使用稳定模板 ID，不把仓库或服务器路径暴露给客户端
- 记录模板版本和兼容的 Pandoc 版本
- 模板资产与预览、许可和测试样例同步维护
- 不允许用户请求执行模板内的任意代码或自定义过滤器

当前阶段不提交二进制模板；默认 reference DOCX 将在 Pandoc 技术验证完成后生成并验收。
